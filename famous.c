#include <stdio.h>
#include "famous.h"

void Todayfamoussaying()
{
        char *buffer[20] = { "No pain No gain", "At the height of laughter the $

        //buffer check
        for (int i = 0; i < 20; i++)
        {
                printf("%s \n",buffer[i]);
        }
}

